# ====================================
# Memoria - 角色模拟系统依赖
# ====================================

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# 数据验证
pydantic==2.9.2
pydantic-settings==2.6.1

# LLM 客户端
openai==1.54.3
httpx==0.25.2

# HTTP 与图像处理
requests==2.34.2
Pillow==12.2.0

# 数据库（默认 SQLite；配置 DATABASE_URL 后使用 PostgreSQL）
# sqlite3 is built-in
psycopg[binary]==3.2.3

# 向量数据库
chromadb==0.4.22
sentence-transformers==2.3.1
# NumPy 版本限制 (chromadb 0.4.22 不支持 NumPy 2.0+)
numpy<2.0,>=1.22.5

# 知识文档解析
pypdf==5.1.0
python-docx==1.1.2

# 测试依赖
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# 开发工具（可选）
# pytest==8.3.3
# black==24.10.0
# ruff==0.7.4
