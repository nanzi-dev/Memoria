# Runtime configuration is defined by memoria.core.config.Configs and loaded
# exclusively from environment variables or the repository-root .env file.
# This compatibility marker intentionally contains no runtime values.
